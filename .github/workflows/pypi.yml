name: "Publish"

on:
  push:
    tags: ['[0-9]+.[0-9]+.[0-9]+']
    branches:
      - 'main'
  # workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    environment:
      name: pypi
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v7
      - name: Sync project
        run: uv sync --dev
      - name: Build
        run: uv build
      - name: Publish
        run: uv publish
